import React from 'react';

const WeatherForm = () => {
    return (
        <form>
            <input type="text" />
            <button type="submit">Search</button>
        </form>
    );
}

export default WeatherForm;